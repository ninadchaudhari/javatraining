package com.demo.products;

import com.demo.products.exceptions.InvalidInputException;

public class ProductApplicationImpl implements ProductApplication {
    @Override
    public String[] searchProduct(String str) throws InvalidInputException {
        return new String[0];
    }
}
